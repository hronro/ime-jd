[package]
name = "jd-cli"
version = "0.1.0"
edition = "2021"

[profile.release]
codegen-units = 1
lto = true
panic = "abort"
strip = true

[dependencies]
crossterm = "0.26"
once_cell = "1"
unicode-width = "0.1"
