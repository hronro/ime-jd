org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.caching=true
android.useAndroidX=true
kotlin.code.style=official
android.nonTransitiveRClass=true
# In-app updater (see app/build.gradle.kts). Off = app-store build, no extra
# permissions. release.yml passes -PjdAutoUpdate=true for the GitHub APKs.
jdAutoUpdate=false
